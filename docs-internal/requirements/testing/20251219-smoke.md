# Smoke Test Results (2025-12-19)

Objective: Confirm new slices stay silent when inactive and activate correctly when matching files are present.

## Test Cases

### 1. Pure Rust CLI
**Expectation**: 
- `devops/container` profile should NOT warn (no Dockerfile).
- `frontend` profile should NOT warn (no package.json/tsconfig).
- `node-monorepo` should NOT warn.

### 2. Next.js Frontend
**Expectation**:
- `frontend` profile SHOULD activate.
- `node-monorepo` SHOULD activate/warn (if strict checks on).
- `devops/container` should NOT warn (unless dockerfile present).

### 3. IaC/Container Stack
**Expectation**:
- `devops/container` SHOULD activate.
- `iac/cloud` SHOULD activate.
- `frontend` should NOT warn.

## Execution Log

